services:
  web:
    build: .
    container_name: asio_web
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www/html
      - ./public:/var/www/html/public
      - ./config:/var/www/html/config
    depends_on:
      - db
    networks:
      - asio_network

  db:
    image: mysql:8.0
    container_name: asio_db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: asio_db
      MYSQL_USER: asio_user
      MYSQL_PASSWORD: asio_pass
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./sql:/docker-entrypoint-initdb.d
    networks:
      - asio_network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: asio_phpmyadmin
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "8081:80"
    depends_on:
      - db
    networks:
      - asio_network

volumes:
  db_data:

networks:
  asio_network:
    driver: bridge
